"use client";
//Data to Fetch:
// All problems (from /api/problems)
// All attempts (from /api/attempts - need to modify to get all attempts)
import { useState } from "react";

export default function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
}
